# pipeline
Scripts for running the treatment effect estimation pipeline.  Files within the `pipeline` directory are ordered alphabetically (e.g., code in `c_find_controls.py` should be run before code in `f_calc_weights.R`). 
